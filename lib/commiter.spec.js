import commiter from './commiter';

describe('Commiter', () => {
    let config;
    let githubApi;

    beforeEach(() => {
        config = {};
        githubApi = {};
    });

    describe('workflow', () => {
        it('should warn the comment author if no fix found');

        it('should create a tree');

        it('shoud create a commit');

        it('should force update branch reference');

        it('should warn the author that the commit is pushed');
    });

    describe('security', () => {
        it('should warn the author if an occured while commiting');
    });
});
