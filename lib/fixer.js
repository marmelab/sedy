module.exports = function fixer(parsedContent) {
    // @todo if parsedContent.matches is empty, comment an error
    // Return data if need to fix
    return {
        'old': null,
        'new': null,
    };
};
